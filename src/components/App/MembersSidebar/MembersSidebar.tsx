import { h } from "preact";
import styles from "../../../styles/App/MembersSidebar.module.css";
export const MembersSidebar = () => {
  return (
    <div className={styles.membersSidebar}>
      <div class={styles.membersSidebarContainer}></div>
    </div>
  );
};
